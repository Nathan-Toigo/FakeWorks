apiVersion: v1
kind: Service
metadata:
  name: svc-redis
  labels:
    app.kubernetes.io/name: pods-rabbitmq
    
spec:
  selector:
    app.kubernetes.io/name: pods-rabbitmq
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379